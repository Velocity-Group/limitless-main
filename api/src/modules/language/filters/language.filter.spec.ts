import { LanguageExceptionFilter } from './language-exception.filter';

describe('LanguageFilter', () => {
  it('should be defined', () => {
    expect(new LanguageExceptionFilter()).toBeDefined();
  });
});
